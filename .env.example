# ============================================================
# Variables de entorno de desarrollo
# Copia este archivo a `.env` y completa los valores reales.
# NUNCA subas `.env` al repositorio (ver .gitignore y 02 §15.10).
# ============================================================

# URL pública del sitio (dominio canónico en producción)
PUBLIC_SITE_URL=http://localhost:4321

# Supabase — credenciales del proyecto (Dashboard > Settings > API)
# Son claves PÚBLICAS (anon) y pueden exponerse al navegador.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# --- SECRETOS (SOLO servidor, NUNCA público) -------------------
# Service role de Supabase. Úsalo exclusivamente en entornos de
# servidor (Server Endpoints del panel admin). No lo expongas a la UI.
# SUPABASE_SERVICE_ROLE_KEY=

# Número de WhatsApp de la marca (formato internacional, solo dígitos)
# PUBLIC_WHATSAPP_NUMBER=51900000000